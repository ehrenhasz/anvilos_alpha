fn main() {
    let welcome = "ANVIL SOVEREIGN INIT (LAW) ACTIVE\n";
    
    // 1. Write Welcome
    unsafe {
        asm!("syscall", in("rax") 1, in("rdi") 1, in("rsi") welcome, in("rdx") 34);
    }

    // 2. Simple spin - In a real kernel we would mount here
    // For Phase 2, we just prove the Law is running.
    loop {
        // The hold is eternal.
    }
}

