{
  "module_name": "sub_and_test",
  "hash_id": "61c31cd06a614049838e1228244a37b627045cb39ea8298463fc697603fbd02d",
  "original_prompt": "Ingested from linux-6.6.14/scripts/atomic/kerneldoc/sub_and_test",
  "human_readable_source": "cat <<EOF\n/**\n * ${class}${atomicname}() - atomic subtract and test if zero with ${desc_order} ordering\n * @i: ${int} value to add\n * @v: pointer to ${atomic}_t\n *\n * Atomically updates @v to (@v - @i) with ${desc_order} ordering.\n *\n * ${desc_noinstr}\n *\n * Return: @true if the resulting value of @v is zero, @false otherwise.\n */\nEOF\n",
  "logic_map": {},
  "failure_modes": [],
  "crash_correlation_map": {}
}