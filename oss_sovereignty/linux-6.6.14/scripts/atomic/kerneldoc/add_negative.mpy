{
  "module_name": "add_negative",
  "hash_id": "cfb8203af64d0e57be107555522a4c8b267968f232dcc5cb3a4d4a9601b86d87",
  "original_prompt": "Ingested from linux-6.6.14/scripts/atomic/kerneldoc/add_negative",
  "human_readable_source": "cat <<EOF\n/**\n * ${class}${atomicname}() - atomic add and test if negative with ${desc_order} ordering\n * @i: ${int} value to add\n * @v: pointer to ${atomic}_t\n *\n * Atomically updates @v to (@v + @i) with ${desc_order} ordering.\n *\n * ${desc_noinstr}\n *\n * Return: @true if the resulting value of @v is negative, @false otherwise.\n */\nEOF\n",
  "logic_map": {},
  "failure_modes": [],
  "crash_correlation_map": {}
}