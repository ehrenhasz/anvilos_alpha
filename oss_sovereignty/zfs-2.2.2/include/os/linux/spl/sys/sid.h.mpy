{
  "module_name": "sid.h",
  "hash_id": "0bcb199895d2d222335a3fee1c3ded09f7b195b81268d46fa1d83aa3618ce849",
  "original_prompt": "Ingested from zfs-2.2.2/include/os/linux/spl/sys/sid.h",
  "human_readable_source": " \n\n#ifndef _SPL_SID_H\n#define\t_SPL_SID_H\n\ntypedef struct ksiddomain {\n\tchar\t\t*kd_name;\n} ksiddomain_t;\n\ntypedef enum ksid_index {\n\tKSID_USER,\n\tKSID_GROUP,\n\tKSID_OWNER,\n\tKSID_COUNT\n} ksid_index_t;\n\ntypedef int ksid_t;\n\nstatic inline ksiddomain_t *\nksid_lookupdomain(const char *dom)\n{\n\tksiddomain_t *kd;\n\tint len = strlen(dom);\n\n\tkd = kmem_zalloc(sizeof (ksiddomain_t), KM_SLEEP);\n\tkd->kd_name = kmem_zalloc(len + 1, KM_SLEEP);\n\tmemcpy(kd->kd_name, dom, len);\n\n\treturn (kd);\n}\n\nstatic inline void\nksiddomain_rele(ksiddomain_t *ksid)\n{\n\tkmem_free(ksid->kd_name, strlen(ksid->kd_name) + 1);\n\tkmem_free(ksid, sizeof (ksiddomain_t));\n}\n\n#endif  \n",
  "logic_map": {},
  "failure_modes": [],
  "crash_correlation_map": {}
}