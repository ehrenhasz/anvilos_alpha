{
  "module_name": "test-ftruncate.c",
  "hash_id": "edb113f06bd8732f61af9ff7495768839d22278bd45d7562f5acfd14717a6c0f",
  "original_prompt": "Ingested from coreutils-9.4/gnulib-tests/test-ftruncate.c",
  "human_readable_source": " \n  {\n    errno = 0;\n    ASSERT (ftruncate (-1, 0) == -1);\n    ASSERT (errno == EBADF);\n  }\n  {\n    close (99);\n    errno = 0;\n    ASSERT (ftruncate (99, 0) == -1);\n    ASSERT (errno == EBADF);\n  }\n\n   \n  {\n    int fd = open (filename, O_RDONLY);\n    ASSERT (fd >= 0);\n    errno = 0;\n    ASSERT (ftruncate (fd, 0) == -1);\n    ASSERT (errno == EBADF || errno == EINVAL\n            || errno == EACCES  \n           );\n    close (fd);\n  }\n\n  return 0;\n}\n",
  "logic_map": {},
  "failure_modes": [],
  "crash_correlation_map": {}
}