{
  "module_name": "mbscasecmp.c",
  "hash_id": "3570e97b5f422ff4ea796dabd18031af77f9c39aadbd5b52c0f9a114a29c8003",
  "original_prompt": "Ingested from bash-5.2.21/lib/sh/mbscasecmp.c",
  "human_readable_source": " \n\n \n\n#include <config.h>\n\n#if !defined (HAVE_MBSCASECMP) && defined (HANDLE_MULTIBYTE)\n\n#include <stdlib.h>\n#include <stddef.h>\n#include <string.h>\n\n#include <wchar.h>\n#include <wctype.h>\n\n \nint\nmbscasecmp (mbs1, mbs2)\n    const char *mbs1;\n    const char *mbs2;\n{\n  int len1, len2, mb_cur_max;\n  wchar_t c1, c2, l1, l2;\n\n  len1 = len2 = 0;\n   \n  (void) mblen ((char *) NULL, 0);\n\n  mb_cur_max = MB_CUR_MAX;\n  do\n    {\n      len1 = mbtowc (&c1, mbs1, mb_cur_max);\n      len2 = mbtowc (&c2, mbs2, mb_cur_max);\n\n      if (len1 == 0)\n\treturn len2 == 0 ? 0 : -1;\n      else if (len2 == 0)\n\treturn 1;\n      else if (len1 > 0 && len2 < 0)\n        return -1;\n      else if (len1 < 0 && len2 > 0)\n        return 1;\n      else if (len1 < 0 && len2 < 0)\n\t{\n\t  len1 = strlen (mbs1);\n\t  len2 = strlen (mbs2);\n\t  return (len1 == len2 ? memcmp (mbs1, mbs2, len1)\n\t\t\t       : ((len1 < len2) ? (memcmp (mbs1, mbs2, len1) > 0 ? 1 : -1)\n\t\t\t\t\t\t: (memcmp (mbs1, mbs2, len2) >= 0 ? 1 : -1)));\n\t}\n\n      l1 = towlower (c1);\n      l2 = towlower (c2);\n \n      mbs1 += len1;\n      mbs2 += len2;\n    }\n  while (l1 == l2);\n\n  return l1 - l2;\n}\n\n#endif\n",
  "logic_map": {},
  "failure_modes": [],
  "crash_correlation_map": {}
}