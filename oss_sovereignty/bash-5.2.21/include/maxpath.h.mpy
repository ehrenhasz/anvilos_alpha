{
  "module_name": "maxpath.h",
  "hash_id": "d6c848ee04fb6b27710a8b3ca8ea9f6dec6c675271f8df9aa9896f1b0c236969",
  "original_prompt": "Ingested from bash-5.2.21/include/maxpath.h",
  "human_readable_source": " \n\n \n\n#if !defined (_MAXPATH_H_)\n#define _MAXPATH_H_\n\n \n#if defined (HAVE_LIMITS_H)\n#  include <limits.h>\n#endif  \n\n \n#if !defined (PATH_MAX)\n#  if defined (HAVE_SYS_PARAM_H)\n#    include <sys/param.h>\n#    define maxpath_param_h\n#  endif\n#  if defined (MAXPATHLEN) && !defined (PATH_MAX)\n#    define PATH_MAX MAXPATHLEN\n#  endif  \n#endif  \n\n \n#if !defined (NAME_MAX)\n#  if defined (HAVE_SYS_PARAM_H) && !defined (maxpath_param_h)\n#    include <sys/param.h>\n#  endif\n#  if defined (MAXNAMLEN) && !defined (NAME_MAX)\n#    define NAME_MAX MAXNAMLEN\n#  endif  \n#endif  \n\n \n#if !defined (PATH_MAX) && defined (_POSIX_PATH_MAX)\n#  define PATH_MAX _POSIX_PATH_MAX\n#endif\n\n#if !defined (NAME_MAX) && defined (_POSIX_NAME_MAX)\n#  define NAME_MAX _POSIX_NAME_MAX\n#endif\n\n\n \n#if !defined (PATH_MAX)\n#  define PATH_MAX 1024\n#endif\n\n#if !defined (NAME_MAX)\n#  define NAME_MAX 14\n#endif\n\n#if PATH_MAX < 1024\n#  undef PATH_MAX\n#  define PATH_MAX 1024\n#endif\n\n#endif  \n",
  "logic_map": {},
  "failure_modes": [],
  "crash_correlation_map": {}
}