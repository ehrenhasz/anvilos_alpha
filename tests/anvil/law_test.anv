fn main() {
    let msg = "ANVIL LAW IS SUPREME\n";
    // x86_64 syscall: write(1, msg, 21)
    // rax=1, rdi=1, rsi=msg, rdx=21
    unsafe {
        asm!("syscall", 
             in("rax") 1, 
             in("rdi") 1, 
             in("rsi") msg, 
             in("rdx") 21);
    }
}

